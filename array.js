
    // Inserindo o array
    const cores = ['azul', 'vermelho', 'amarelo', 'verde', 'rosa'];

    // Executando o loop for para imprimir cada cor
    for (let i = 0; i < cores.length; i++) {
      document.write(`Minha escolha ${i+1} é ${cores[i]} <br>`);
    }

